export default function AnnouncementBar() {
    return (
        <div className="fixed top-0 left-0 w-full z-60 bg-red-800 text-white text-center text-xs py-2 tracking-widest uppercase">
            Free shipping on orders over $150 AUD
        </div>
    );
}
